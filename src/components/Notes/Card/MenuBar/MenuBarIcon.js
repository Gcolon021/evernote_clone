import React from 'react'
import * as S from "../../../../styledComponents/NoteCard/MenuBarIcon";

const MenuBarIcon = ({ children }) => {
    return (
        <S.MenuBarIcon>{children}</S.MenuBarIcon>
    )
}

export default MenuBarIcon
