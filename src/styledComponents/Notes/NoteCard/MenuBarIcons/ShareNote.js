import styled from "styled-components";
import SvgIcon from '@material-ui/core/SvgIcon';

export const ShareNote = styled(SvgIcon)`
    && {
        color: transparent;
    }

    &&:hover {
        fill: #FFFFFF !important;
    }
`;
